# Emnist-Handwritten-Character-Classifier
A Streamlit web app that uses a CNN trained on the EMNIST ByClass dataset to recognize handwritten digits (0–9) and alphabets (A–Z, a–z). Supports drawing on a canvas or uploading an image, with top-5 predictions and real-time inference.
